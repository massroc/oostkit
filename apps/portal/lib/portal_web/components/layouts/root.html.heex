<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <meta
      name="description"
      content={
        assigns[:meta_description] ||
          "Practical online tools built on Open Systems Theory — design organisations where people manage their own work."
      }
    />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="OOSTKit" />
    <meta
      property="og:title"
      content={(assigns[:page_title] || "Home") <> " — OOSTKit"}
    />
    <meta
      property="og:description"
      content={
        assigns[:meta_description] ||
          "Practical online tools built on Open Systems Theory — design organisations where people manage their own work."
      }
    />
    <.live_title suffix=" — OOSTKit">
      {assigns[:page_title] || "Home"}
    </.live_title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&display=swap"
      rel="stylesheet"
    />
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-surface-wall font-brand antialiased">
    <header class="bg-ok-purple-900">
      <nav class="mx-auto flex max-w-7xl items-center justify-between p-4 lg:px-8">
        <!-- Left: Brand -->
        <a href="/" class="text-lg font-semibold text-white">
          OOSTKit
        </a>
        
<!-- Centre: Page title -->
        <span class="hidden text-sm font-medium text-ok-purple-200 sm:inline">
          {assigns[:page_title] || "Dashboard"}
        </span>
        
<!-- Right: Auth -->
        <div class="flex items-center gap-4">
          <%= if @current_scope && @current_scope.user do %>
            <span class="hidden text-sm text-ok-purple-300 sm:inline">
              {@current_scope.user.email}
            </span>
            <%= if @current_scope.user.role == "super_admin" do %>
              <a
                href="/admin"
                class="text-sm font-semibold text-ok-gold-300 hover:text-ok-gold-200"
              >
                Admin
              </a>
            <% end %>
            <a
              href="/users/settings"
              class="text-sm font-semibold text-ok-purple-200 hover:text-white"
            >
              Settings
            </a>
            <.link
              href={~p"/users/log-out"}
              method="delete"
              class="text-sm font-semibold text-ok-purple-200 hover:text-white"
            >
              Log out
            </.link>
          <% else %>
            <%= if @dev_mode do %>
              <.link
                href="/dev/admin-login"
                method="post"
                class="text-sm font-semibold text-ok-gold-300 hover:text-ok-gold-200"
              >
                Admin
              </.link>
            <% end %>
            <a
              href="/users/register"
              class="rounded-md bg-white/10 px-3 py-1.5 text-sm font-semibold text-white hover:bg-white/20"
            >
              Sign Up
            </a>
            <a
              href="/users/log-in"
              class="text-sm font-semibold text-ok-purple-200 hover:text-white"
            >
              Log In
            </a>
          <% end %>
        </div>
      </nav>
    </header>
    <div class="brand-stripe"></div>
    {@inner_content}
  </body>
</html>
