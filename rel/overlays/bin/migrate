#!/bin/sh
set -eu

cd -P -- "$(dirname -- "$0")"

echo "Running database migrations..."
./productive_workgroups eval "ProductiveWorkgroups.Release.migrate()"

echo "Running database seeds..."
./productive_workgroups eval "ProductiveWorkgroups.Release.seed()"

echo "Database setup complete."
